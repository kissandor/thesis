 <Grid>
     <DataGrid x:Name="DataGridWebShops" 
                 Margin="10,50,10,10"
                 AutoGenerateColumns="False"
                 ItemsSource="{Binding WebShops}"
                 Style="{StaticResource DataGridStyle}">
	<!-- ...  -->
	</DataGrid> 
	<!-- ...  -->
</Grid>
     